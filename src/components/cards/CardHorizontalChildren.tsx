import { ReactNode } from "react";

interface Props {
  children: ReactNode;
}

function CardHorizontalChildren ({children}: Props) {
  return (
    <div className="card">
      
    </div>
  )
};

export default CardHorizontalChildren;